# 更新日志

[English →](CHANGELOG.md)

## 0.2.2 — 2026-09-19

适配新版 Claude Code，加快差异加载，让工具界面更清爽。

- 恢复 Claude Code 2.1.278 兼容性，改善更新后的适配能力。
- 修复未安装可选 companion 扩展时 Selection / File 引用失效的问题。
- 加快 diff 显示，让子代理历史保持流畅；工具样式统一，单页可容纳更多记录。
- 三套主题统一实心工具背景、紧凑标题、均衡的差异搜索间距与复制动效，界面控件全部使用英文。
- 精简输入区：合并缓存统计和倒计时，将 Agent map 移到顶部增删统计旁，并隐藏重复的宿主控件。

## 0.2.1 — 2026-09-12

更快的 diff，更顺畅的长会话。

- 文件工具折叠时保持轻量，展开后按页加载差异。
- 历史结果到达后自动恢复 diff 预览，无需手动刷新。
- 子代理输出只在所属工具调用内显示，不再重复出现在主会话中。
- 修复历史记录处理可能导致长会话中停止按钮迟迟不生效的问题。

## 0.2.0 — 2026-09-07

这一版的主角是 transcript。工具调用、thinking 和 diff 原本是一叠彼此独立的卡片，现在收成一条活动列——本来就该是这个样子。下面大部分内容都由此而来，另外还有一组安装侧的修复，堵掉了 incipit 可能把 Claude Code 面板搞白的那条路径。

### Transcript

- 工具调用、thinking 和它们的预览合成一条活动轨道：每步一个字形、字形之间一条连接线、首行一个可折叠整组的标题——*Read 7 files, edited 5 files · 1 failed*。每行给出时态正确的动作名、文件名、`+/−` 统计和宿主自己的结果行；进行中的步骤闪烁，失败的变红。
- 工具之间 Claude 说的话仍然是普通正文，并且会切断一组——收起一组只隐藏"怎么做的"，不会藏起 Claude 对你说的任何一句话。
- 宿主渲染为空或被隐去的 thinking 块，现在也进轨道、跟随整组折叠，不再孤零零地悬在那里。
- 阅读栏按字符数而不是像素定宽，任何字号下版心都稳定；侧栏宽度的窄面板两侧也留得出真正的空气。
- 折叠与悬浮一律有动效——折叠 220ms、悬浮 180ms——并且绝不发生在流式渲染路径上。

### Diff 预览

- 只剩一个带边框的视口，整行的增删底色一直铺到边框。标题、提示块和重复的文件名都去掉了，翻页与 *Full diff* 收进一行页脚。
- 历史 diff 按需从记录下来的补丁算出，带翻页与缓存；确实无内容可显示时给出明确状态，而不是空白。
- 编辑统计在工具刚跑完时就先出来，真实补丁到达后替换；结果还没落盘的会重试，不会无限期干等。
- 语法高亮与正文共用一套，语言按需加载，也不再启动那个会被面板 CSP 拦掉的 worker。

### 子代理与工作流

- 首次可以看到嵌套的子代理活动与工作流进度：与会话绑定的只读代理历史、观测到的任务事件、按阶段展示的工作流视图，终止状态显式给出。

### 额度徽章

- 缓存弹窗围绕它的折线图重做。纵轴现在直接报出所选区间自身的最高与最低命中率，虚线给出均值，于是那几张重复这些数字的统计卡整组退休；区间摘要收成一行，下面一条覆盖全部计费类目的条。高度约为原来的一半，窄到 234px 的侧栏里依然可读。

### 滚动与输入

- 只有在你自己没有往上翻的时候，transcript 才跟随新输出；中键、滚轮和键盘浏览都被尊重，而不是被"抢"回去。
- 修掉长输入底部的幽灵行——可见文字整体上移一行、光标落在错行、退格删不掉。（这是 Chromium 与宿主的交互问题，在未打补丁的 Claude Code 上同样复现。）

### 安装与恢复

- 宿主 bundle 改为原子写入，且两个 bundle 都打完补丁并证明可解析之后才落盘。进程被杀、断电、杀软中途持锁，都不会再留下一个被截断的文件和一块白屏面板。
- @ 引用桥重新锚定到 Claude Code 2.1.231 及以后的形态，当前版本上的 companion 文件引用恢复可用。
- 登记了 Claude Code 2.1.220、2.1.231、2.1.251、2.1.258 的宿主路由指纹。

### 移除

- incipit 不再测量或改写 *Thought for Ns*。这个数字在 Claude Code 里来自一个只存在于运行时的时钟，任何地方都没有持久化的时长；因此从历史加载的块上，incipit 显示的数字是它自己编的。现在这行文字完全交回宿主。

### 升级方式

```bash
npm install -g incipit@latest
incipit apply
```

然后在 VS Code 里执行 **Reload Window**。

---

从 0.2.0 起，发布由 CI 在 annotated tag 推送后构建并发出，附带 npm provenance 证明。0.2.0 之前的版本记录在 git 历史里。
